const tailwind = require('tailwindcss');
const autoprefixer = require('autoprefixer');
const tailwindConfig = require('./src/css/tailwind.config.mjs');

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  }
};